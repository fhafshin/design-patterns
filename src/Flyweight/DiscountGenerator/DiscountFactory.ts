export default class DiscountFactory {
  public createDiscount(
    _code: string,
    _user: number,
    _percent: number,
    _minAmount: number,
    _maxAmount: number,
    _limit: number,
    _expireDate: Date
  ) {}
}
